package ru.sstu.htmlpages.services;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.web.reactive.function.client.WebClient;

/*@Service
@RequiredArgsConstructor*/
public class fromGatewayService {

    /*private final WebClient webClient;

    public String getUsername() {
        return webClient.get().uri("/get_username").retrieve().bodyToMono(String.class).block();
    }

    public String getToken() {
        return webClient.get().uri("/get_token").retrieve().bodyToMono(String.class).block();
    }*/

}
